# Config prettier the kungog way

## Usage

**Install**:

```bash
$ npm install --save-dev kungog-prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "kungog-prettier-config"
}
```
